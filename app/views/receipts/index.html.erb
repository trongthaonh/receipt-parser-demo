<p id="notice"><%= notice %></p>

<h1>Receipts</h1>

<table>
  <thead>
    <tr>
      <th>ID</th>
      <th>Purchase date</th>
      <th>Phone number</th>
      <th>Purchase date is correct</th>
      <th>Phone number is correct</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @receipts.each do |receipt| %>
      <tr>
        <td><%= receipt.id %></td>
        <td><%= receipt.purchase_date %></td>
        <td><%= receipt.phone_number %></td>
        <td><%= receipt.purchase_date_is_correct %></td>
        <td><%= receipt.phone_number_is_correct %></td>
        <td><%= link_to 'Show', receipt %></td>
        <td><%= link_to 'Edit', edit_receipt_path(receipt) %></td>
        <td><%= link_to 'Destroy', receipt, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Receipt', new_receipt_path %>
